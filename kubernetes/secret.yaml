# WARNING: This file is a TEMPLATE for local/demo use only.
# Never commit real credentials to Git. For this learning project the
# values below are placeholders. In production, create secrets directly
# with `kubectl create secret` (see docs/deployment-guide.md) or use a
# secrets manager (e.g. Sealed Secrets, External Secrets Operator, Vault).
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-secret
  namespace: taskflow
type: Opaque
stringData:
  DB_USER: demo_user
  DB_PASSWORD: CHANGE_ME_LOCAL_ONLY
