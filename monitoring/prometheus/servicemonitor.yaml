# Optional: only works if you add an nginx metrics exporter sidecar.
# By default, plain nginx has no /metrics endpoint, so this ServiceMonitor
# is provided as a template for when you extend the app with metrics.
#
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: taskflow-monitor
#   namespace: monitoring
#   labels:
#     release: prometheus
# spec:
#   namespaceSelector:
#     matchNames:
#       - taskflow
#   selector:
#     matchLabels:
#       app: taskflow
#   endpoints:
#     - port: metrics
#       interval: 15s
